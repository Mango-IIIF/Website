{
  "name": "mango-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@mango-iiif/iiif-viewer": "^0.5.3",
    "vue": "^3.5.38",
    "vue-router": "^5.1.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.47.0",
    "@vitejs/plugin-vue": "^6.0.7",
    "vite": "^8.1.0",
    "wrangler": "^4.114.0"
  }
}
